<?xml version="1.0" encoding="iso-8859-1"?>

<root>

	<windowclass name="reference_equipmenttable">
		<script>
			function onInit()
				local sModule = DB.getModule(getDatabaseNode());
				ListManager.toggleRecordView("item", "equipment", "reference.equipment@" .. sModule);
				close();
			end
		</script>
		<sheetdata />
	</windowclass>

	<!-- <windowclass name="reference_loadouttable"> -->
		<!-- <script> -->
			<!-- function onInit() -->
				<!-- local sModule = DB.getModule(getDatabaseNode()); -->
				<!-- ListManager.toggleRecordView("item", "loadout", "reference.loadout@" .. sModule); -->
				<!-- close(); -->
			<!-- end -->
		<!-- </script> -->
		<!-- <sheetdata /> -->
	<!-- </windowclass> -->

	<windowclass name="reference_weapontable">
		<script>
			function onInit()
				local sModule = DB.getModule(getDatabaseNode());
				ListManager.toggleRecordView("item", "weapon", "reference.weapon@" .. sModule);
				close();
			end
		</script>
		<sheetdata />
	</windowclass>

	<windowclass name="reference_armortable">
		<script>
			function onInit()
				local sModule = DB.getModule(getDatabaseNode());
				ListManager.toggleRecordView("item", "armor", "reference.armor@" .. sModule);
				close();
			end
		</script>
		<sheetdata />
	</windowclass>

</root>
